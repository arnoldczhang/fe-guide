# 前后端统一监控方案

## 方案

- TraceId
- SessionId
- ActionId
- 错误指纹

|      | TraceId                                                               | SessionId                                            | ActionId                                                    | 错误指纹             |
| ---- | --------------------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------- | ---------------- |
| 时效性  | 单次请求                                                                  | 30分钟-2小时                                             | 单次关键操作请求                                                    | 单次错误             |
| 生成方式 | 1. 前端生成TraceId，通过`x-trace-id`传给后端<br/><br/>2. 后端透传TraceId，在日志和监控数据中关联 | 1. 前端生成SessionId，所有行为（错误、性能、点击等交互）与后端请求通过SessionId关联 | 1. 前端在关键操作时生成唯一操作标记（ActionId），给后端<br/><br/>2. 后端记录到日志中做后续关联 | md5(error stack) |
| 优点   | 全链路追踪                                                                 | 时间段的聚合分析                                             | 关键操作归因                                                      | 高频错误归因           |
| 缺点   | 前后端都有改动                                                               | 无法精确到单次操作                                            | 属于自定义埋点，要手工埋；无法覆盖其他类型事件（比如静态资源异常）                           | 上下文缺失            |

---

## 分层

1. 数据采集层
2. 传输层
3. 存储层
4. 计算层
5. 应用层

### 1. 数据采集层

前后端sdk（前端sendBean、后端中间件）

### 2. 传输层

kafka统一接收数据，缓冲削峰

### 3. 存储层

clickhouse、物化视图

### 4. 计算层

实时计算 - flink
离线计算 - hive/spark

### 5. 应用层

可视化平台、告警系统、根因工具

---

## sdk设计

1. 数据采集
2. 数据处理
3. 数据上报
4. 数据存储

### 1. 数据采集

#### 自动采集
- 代理全局事件
- 代理全局对象

```js
// 代理全局事件
document.addEventListener('click' (e) => {
  e.target;
});

// 路由监听（SPA兼容）
history.pushState = new Proxy(history.pushState, {
  apply: (target, thisArg, args) => {
    track('pageview', {url: args[2]});
    return target.apply(thisArg, args);
  }
});
```

#### 手动采集
```js
sdk.track(eventName, payload);
```

### 2. 数据处理
- 数据格式化
- 数据压缩

#### 数据格式化
```js
{
  event: "click",
  timestamp: 1620000000000,
  user: { id: "U123", deviceId: "D456" },
  page: { url: "/home", title: "首页" },
  properties: { xPath: "//div[@id='btn']", text: "提交" }
}
```

#### 数据压缩
> JSON.stringify + Snappy压缩（WebWorker）

fflate在包大小（3k）和压缩率（75%）方面，是众多方案中最优的。

```js
import { strToU8, gzipSync } from 'fflate';

// Snappy兼容的压缩方法
const snappyCompress = (data) => {
  // 转换为Uint8Array
  const input = typeof data === 'string' 
    ? strToU8(data) 
    : data;
  // 使用fflate的gzip实现（压缩级别6为最佳平衡点）
  const compressed = gzipSync(input, { level: 6 });
  // 添加Snappy格式头（4字节幻数）
  const header = new Uint8Array([0xff, 0x06, 0x00, 0x00]);
  return new Uint8Array([...header, ...compressed]);
};

self.onmessage = (e) => {
  const { data, id } = e.data;
  try {
    const compressed = snappyCompress(data);
    self.postMessage({ id, compressed }, [compressed.buffer]);
  } catch (err) {
    self.postMessage({ id, error: err.message });
  }
};
```

### 3. 数据上报
- 智能发送策略
- 失败处理

#### 智能发送策略
```js
function sendData(payload) {
  if (!navigator.online) return;
  if (document.visibilityState === 'hidden') {
    navigator.sendBeacon(endpoint, payload);
  } else {
    fetch(endpoint, {
      method: 'POST',
      body: data,
      signal: AbortController.timeout(3000),
    });
  }
}
```

##### sendBeacon

**优势**

- 页面卸载时，不会中断
- 异步，不阻塞渲染和页面跳转
- 自动处理content-type

**劣势**

- 只能是post
- 无法处理响应
- 数据大小限制64k

#### 失败处理
- 指数级重试（2s、4s、8s）
- 数据保质期（24小时清除）

### 4. 数据存储
- IndexedDB
- LRU缓存

```js
class Storage {
  constructor() {
    this.primaryCach = new IndexedDB('log'); // 主缓存
    this.memoryCach = new LRUCache(50); // 临时缓存
  }

  async add(log) {
    this.memoryCach.add(log);
    // 批量持久化
    if (this.memoryCach.size > 10) {
      this.primaryCach.add(this.memoryCach);
    }
  }
}
```

### 性能优化
- 采样率
- WebWorker异步计算

### 安全合规

#### 数据脱敏
```js
function sanitize(data) {
  // 过滤敏感字段
  const SENSITIVE_KEYS = ['password', 'creditCard'];
  return deepFilter(data, key => !SENSITIVE_KEYS.includes(key));
}
```

#### 隐私控制
```js
if (navigator.doNotTrack) {
  disableTracking();
}
```
