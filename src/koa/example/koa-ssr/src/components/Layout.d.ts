import * as React from "react";
import { BaseState, BaseProps } from "../types";
declare class Layout extends React.Component<BaseProps, BaseState> {
    constructor(props?: {});
    render(): JSX.Element;
}
export default Layout;
