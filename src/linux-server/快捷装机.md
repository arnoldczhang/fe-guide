# 快捷装机

```sh
# zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.35.2/install.sh | bash
source ~/.zshrc

# nrm
source ~/.zshrc

# smartgit
# 19.1.8 #14230

# git
# 直接下载
```

## mac快捷装机

### homebrew
> 包管理工具

```sh
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### git

```sh
brew install git
```

**smartgit**

https://download.smartgit.dev/smartgit/archive/smartgit-macosx-19_1_8.dmg

**账号**

arnoldzhang

**密码**

看微信

### nvm
> node版本管理工具

**安装**

```sh
brew install nvm
```

**添加全局命令**

```sh
echo 'export NVM_DIR="$HOME/.nvm"' >> ~/.zshrc
echo '[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"' >> ~/.zshrc

source ~/.zshrc   # 如果您使用 Zsh
# 或者
source ~/.bash_profile # 如果您使用 Bash
```

**装版本**

```sh
nvm install 22.12.0
nvm install 20.19.4
nvm install 18.19.0

nvm alias default 22.12.0 # 将当前使用的版本设置为默认
```

**node**

```sh
npm install -g pnpm
npm install -g yarn
```

### nrm
> npm仓库镜像管理

```sh
npm install -g nrm

nrm use cnpm
```

### 翻墙

**clash**

安装包：https://dl.trojan-cdn.com/trojan/universal/clashvr/Clash.Verge_aarch64.dmg

**地址**

看微信

**设置方法**

https://secure.shadowsocks.au/knowledgebase/202/Windows-or-macOS-or-LinuxClash-Verge-Rev-%E8%AE%BE%E7%BD%AE%E6%96%B9%E6%B3%95.html

### typora

**0.11.18 beta**

https://www.cnblogs.com/ioufev/articles/15610121.html

### vscode

### chrome

### github

authenticator

---

## windows快捷装机

### 虚拟机
wsl --install
sudo apt update && sudo apt upgrade

### zsh

```sh
sudo apt install zsh
chsh -s $(which zsh)
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-\~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions
git clone https://github.com/zsh-users/zsh-syntax-highlighting ${ZSH_CUSTOM:-\~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting
vim ~/.zshrc
plugins=(git zsh-autosuggestions zsh-syntax-highlighting)
```
